version: "3.0"
services:
  web:
    build: ./app
    env_file: ./app/dev.env

  nginx:
    ports:
      - 8080:80
      - 4443:443
    env_file: ./nginx/dev.env
    healthcheck:
      test: ["CMD", "curl", "-f", "https://localhost"]
      interval: 1m30s
      timeout: 10s
      retries: 3
