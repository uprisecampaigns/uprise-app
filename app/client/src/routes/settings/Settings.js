import React, { PropTypes } from 'react';


class Settings extends React.Component {
  static propTypes = {
  };

  render() {
    return (
      <div>
        <h1>Settings</h1>
      </div>
    );
  }
}

export default Settings;
